{
  "name": "relustt-web",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "quiz:catalog": "node scripts/build-quiz-catalog.cjs",
    "preview:tracking": "node scripts/preview-tracking.cjs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8"
  }
}
